[phases.setup]
nixPkgs = ["python312", "ffmpeg"]
aptPkgs = ["libpq-dev"]

[phases.install]
cmds = ["python3.12 -m venv /app/.venv && /app/.venv/bin/pip install -r requirements.txt"]

[start]
cmd = "gunicorn src.main:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
